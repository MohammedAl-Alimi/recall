# recall

recall is a terminal session manager for the Claude Code CLI. It catalogs every session found under the Claude projects directory, shows each one with its title, outcome and current state, and reopens a session by focusing its terminal tab, attaching a kept tmux session, or running `claude --resume`. This README is a placeholder while the project is scaffolded.
